// const mission = process.argv[1]; //? process the input from terminal
// const { get } = require("https"); //? get / request if we use http we uese request if we use https we use get when we use get we don't need to end by using req.end()

// const req = get("https://www.google.com", (res) => {
// 	res.on("data", (data) => {
// 		console.log(data);
// 	});
// 	res.on("end", () => {
// 		console.log("no more data");
// 	});
// });

//
